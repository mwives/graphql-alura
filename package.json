{
  "name": "graphql-alura",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "scripts": {
    "json-server": "json-server --watch db.json --port 3001"
  }
}
